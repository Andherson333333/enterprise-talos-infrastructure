tls:
  certificates:
    - certFile: /certs/gitea.pem
      keyFile: /certs/gitea.key
  stores:
    default:
      defaultCertificate:
        certFile: /certs/gitea.pem
        keyFile: /certs/gitea.key
